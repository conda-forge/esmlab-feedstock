{% set name = "esmlab" %}
{% set version = "2019.2.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 4182f1c85246e203cbd8e031e55e90b0dfa5ec8f70227e70de8495ccf824680d
  

build: 
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  noarch: python
  number: 0

requirements:

  host:
    - python
    - pip

  run:
    - python
    - xarray
    - cftime
    - dask
    - netcdf4

test:
  source_files:
    - tests
  requires:
    - pytest
  imports:
    - esmlab
  commands:
    - pytest --verbose

about:
  
  home: https://github.com/NCAR/esmlab
  license: Apache 2.0
  license_file: LICENSE
  summary: |
    Tools for working with earth system multi-model analyses with xarray. 
  dev_url: https://github.com/NCAR/esmlab
  doc_url: https://esmlab.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - andersy005
    - kmpaul
    - jukent
    - matt-long
